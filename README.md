Учебный проект — реализация системы LMS (Learning Management System) с использованием **Django REST Framework**.  
Проект создан в рамках домашних заданий по теме **ViewSet, Generic, SerializerMethodField и фильтрация данных**.

---

## 🚀 Функционал проекта

### 🧩 Приложение `lms`
**Курс (`Course`)**
- `title` — название курса  
- `description` — описание  
- `preview` — превью (картинка, опционально)  
- `lessons_count` — количество уроков (через `SerializerMethodField`)  
- `lessons` — вложенный список уроков (через сериализатор `LessonSerializer`)

**Урок (`Lesson`)**
- `title` — название урока  
- `description` — краткое описание  
- `preview` — превью (картинка, опционально)  
- `video_url` — ссылка на видео  
- `course` — связь с курсом (ForeignKey)

---

### 👤 Приложение `users`
**Пользователь (`User`)**
- Авторизация по `email`  
- Доп. поля: `phone`, `city`, `avatar`

**Платёж (`Payment`)**
- `user` — пользователь  
- `date` — дата оплаты  
- `course` — ссылка на курс (если оплачен курс)  
- `lesson` — ссылка на урок (если оплачен отдельно урок)  
- `amount` — сумма оплаты  
- `payment_method` — способ оплаты (`cash` / `transfer`)

---

## ⚙️ API эндпоинты

| Метод | Эндпоинт | Описание |
|-------|-----------|-----------|
| `GET` | `/api/courses/` | Список всех курсов |
| `POST` | `/api/courses/` | Создать новый курс |
| `GET` | `/api/courses/{id}/` | Получить один курс |
| `PATCH` | `/api/courses/{id}/` | Изменить курс |
| `DELETE` | `/api/courses/{id}/` | Удалить курс |
| `GET` | `/api/lessons/` | Список всех уроков |
| `POST` | `/api/lessons/` | Создать урок |
| `GET` | `/api/payments/` | Список платежей с фильтрацией и сортировкой |

---

## 🔍 Фильтрация платежей

Поддерживаются параметры:

| Параметр | Пример | Описание |
|-----------|--------|----------|
| `ordering` | `?ordering=-date` | Сортировка по дате оплаты |
| `course` | `?course=1` | Фильтрация по ID курса |
| `lesson` | `?lesson=2` | Фильтрация по ID урока |
| `payment_method` | `?payment_method=cash` | Фильтрация по способу оплаты |